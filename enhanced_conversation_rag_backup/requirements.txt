# MacBook Air 2017 Optimized Requirements
# Intel Core i5, 8GB RAM, macOS Monterey

# Core RAG Dependencies
pandas==2.0.3
numpy==1.24.3
scipy==1.11.4
scikit-learn==1.3.2
torch==2.0.1
transformers==4.33.0
huggingface_hub==0.16.4
sentence-transformers==2.2.2
faiss-cpu==1.7.4
google-generativeai==0.3.2
python-dotenv==1.0.0

# Moshi Conversational AI Dependencies
moshi_mlx>=0.1.0           # MLX optimized for Mac
aiohttp>=3.8.0             # Async HTTP operations
websockets>=11.0.0         # Real-time communication
asyncio-mqtt>=0.16.0       # Async operations
pyttsx3>=2.90              # Text-to-speech fallback

# Audio Processing (Optional - Moshi handles most)
pyaudio>=0.2.11            # Audio I/O
sounddevice>=0.4.6         # Audio device interface

# Memory-optimized settings
# Use CPU-only versions (no CUDA on Intel Mac)
# Smaller batch sizes recommended for 8GB RAM
# Moshi MLX optimized for Apple Silicon/Intel Mac